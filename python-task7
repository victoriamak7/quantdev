#**Create a function called avg_price that takes in a list of stock
#price numbers and calculates the average (Sum of the numbers divided
#by the number of elements in the list). It should return a float. **

def avg_price(inp):
    return sum(inp) / len(inp)

print(avg_price([3,4,5]))
